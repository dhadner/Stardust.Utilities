// <auto-generated>
// This code was generated by Stardust.Generators source generator.
// </auto-generated>
#nullable enable
using System;
using System.Buffers.Binary;
using System.Runtime.CompilerServices;
using System.Text.Json;
using System.Text.Json.Serialization;
using Stardust.Utilities;

namespace Stardust.Utilities.Tests;

public partial class BitFieldsViewTests
{
    [JsonConverter(typeof(SignedNarrowViewJsonConverter))]
    public partial record struct SignedNarrowView
    {
        private readonly Memory<byte> _data;
        private readonly byte _bitOffset;

        /// <summary>Minimum number of bytes required in the backing buffer.</summary>
        public const int SIZE_IN_BYTES = 3;
        public const int BIT_WIDTH = 24;

        /// <summary>Creates a view over the specified memory buffer.</summary>
        /// <param name="data">The buffer to view. Must contain at least <see cref="SIZE_IN_BYTES"/> bytes.</param>
        /// <exception cref="ArgumentException">The buffer is too short.</exception>
        public SignedNarrowView(Memory<byte> data)
        {
            if (data.Length < SIZE_IN_BYTES)
                throw new ArgumentException($"Buffer must contain at least {SIZE_IN_BYTES} bytes, but was {data.Length}.", nameof(data));
            _data = data;
            _bitOffset = 0;
        }

        /// <summary>Creates a view over the specified byte array.</summary>
        [MethodImpl(MethodImplOptions.AggressiveInlining)]
        public SignedNarrowView(byte[] data) : this(data.AsMemory()) { }

        /// <summary>Creates a view over a portion of the specified byte array.</summary>
        [MethodImpl(MethodImplOptions.AggressiveInlining)]
        public SignedNarrowView(byte[] data, int offset) : this(data.AsMemory(offset)) { }

        /// <summary>Creates a sub-view at a bit offset within the specified memory buffer (used by nested views).</summary>
        internal SignedNarrowView(Memory<byte> data, int bitOffset)
        {
            _data = data;
            _bitOffset = (byte)bitOffset;
        }

        /// <summary>Gets the underlying memory buffer.</summary>
        public Memory<byte> Data => _data;

        public partial short Narrow8
        {
            [MethodImpl(MethodImplOptions.AggressiveInlining)]
            get
            {
                var s = _data.Span;
                if (_bitOffset == 0)
                {
                    return (short)s[0];
                }
                int ep = 0 + _bitOffset;
                int bi = ep >> 3;
                int endInWindow = (ep + 7) - bi * 8;
                int sh = 16 - 1 - endInWindow;
                return (short)((BinaryPrimitives.ReadUInt16BigEndian(s.Slice(bi)) >> sh) & 0x00FF);
            }
            [MethodImpl(MethodImplOptions.AggressiveInlining)]
            set
            {
                var s = _data.Span;
                if (_bitOffset == 0)
                {
                    s[0] = (byte)value;
                }
                else
                {
                    int ep = 0 + _bitOffset;
                    int bi = ep >> 3;
                    int endInWindow = (ep + 7) - bi * 8;
                    int sh = 16 - 1 - endInWindow;
                    var slice = s.Slice(bi);
                    ushort raw = BinaryPrimitives.ReadUInt16BigEndian(slice);
                    ushort m = (ushort)(0x00FF << sh);
                    raw = (ushort)((raw & (ushort)~m) | (((ushort)value << sh) & m));
                    BinaryPrimitives.WriteUInt16BigEndian(slice, raw);
                }
            }
        }

        public partial short Full16
        {
            [MethodImpl(MethodImplOptions.AggressiveInlining)]
            get
            {
                var s = _data.Span;
                if (_bitOffset == 0)
                {
                    return (short)(BinaryPrimitives.ReadUInt16BigEndian(s.Slice(1)) & 0xFFFF);
                }
                int ep = 8 + _bitOffset;
                int bi = ep >> 3;
                int endInWindow = (ep + 15) - bi * 8;
                int sh = 32 - 1 - endInWindow;
                return (short)((BinaryPrimitives.ReadUInt32BigEndian(s.Slice(bi)) >> sh) & 0xFFFFU);
            }
            [MethodImpl(MethodImplOptions.AggressiveInlining)]
            set
            {
                var s = _data.Span;
                if (_bitOffset == 0)
                {
                    var slice = s.Slice(1);
                    ushort raw = BinaryPrimitives.ReadUInt16BigEndian(slice);
                    raw = (ushort)((raw & 0x0000) | ((ushort)value & 0xFFFF));
                    BinaryPrimitives.WriteUInt16BigEndian(slice, raw);
                }
                else
                {
                    int ep = 8 + _bitOffset;
                    int bi = ep >> 3;
                    int endInWindow = (ep + 15) - bi * 8;
                    int sh = 32 - 1 - endInWindow;
                    var slice = s.Slice(bi);
                    uint raw = BinaryPrimitives.ReadUInt32BigEndian(slice);
                    uint m = (uint)(0xFFFFU << sh);
                    raw = (uint)((raw & (uint)~m) | (((uint)value << sh) & m));
                    BinaryPrimitives.WriteUInt32BigEndian(slice, raw);
                }
            }
        }

        /// <summary>Metadata for every field and flag declared on this view, in declaration order.</summary>
        public static ReadOnlySpan<BitFieldInfo> Fields => new BitFieldInfo[]
        {
            new("Narrow8", 0, 8, "short", false, ByteOrder.BigEndian, BitOrder.BitZeroIsMsb, StructTotalBits: 24, FieldMustBe: 0),
            new("Full16", 8, 16, "short", false, ByteOrder.BigEndian, BitOrder.BitZeroIsMsb, StructTotalBits: 24, FieldMustBe: 0),
        };

        private sealed class SignedNarrowViewJsonConverter : JsonConverter<SignedNarrowView>
        {
            /// <summary>Reads a SignedNarrowView from a JSON hex string.</summary>
            public override SignedNarrowView Read(ref Utf8JsonReader reader, Type typeToConvert, JsonSerializerOptions options)
            {
                var s = reader.GetString();
                if (s is null) return new SignedNarrowView(new byte[SIZE_IN_BYTES]);
                ReadOnlySpan<char> hex = s.AsSpan();
                if (hex.Length >= 2 && hex[0] == '0' && (hex[1] == 'x' || hex[1] == 'X'))
                    hex = hex.Slice(2);
                var bytes = new byte[SIZE_IN_BYTES];
                int hexLen = hex.Length;
                for (int i = 0; i < SIZE_IN_BYTES && (hexLen - i * 2) > 0; i++)
                {
                    int hi = hexLen - (i + 1) * 2;
                    if (hi >= 0)
                        bytes[i] = (byte)((HexVal(hex[hi]) << 4) | HexVal(hex[hi + 1]));
                    else if (hi == -1)
                        bytes[i] = (byte)HexVal(hex[0]);
                }
                return new SignedNarrowView(bytes);
            }

            /// <summary>Writes a SignedNarrowView to JSON as a hex string.</summary>
            public override void Write(Utf8JsonWriter writer, SignedNarrowView value, JsonSerializerOptions options)
            {
                var s = value._data.Span;
                // Find highest non-zero byte for minimal hex output
                int top = SIZE_IN_BYTES - 1;
                while (top > 0 && s[top] == 0) top--;
                // Build hex string from most-significant to least-significant byte
                var chars = new char[2 + (top + 1) * 2];
                chars[0] = '0';
                chars[1] = 'x';
                const string HEX_CHARS = "0123456789ABCDEF";
                // First byte (top) may have leading zero nibble -- include both for simplicity
                for (int i = top; i >= 0; i--)
                {
                    int pos = 2 + (top - i) * 2;
                    chars[pos] = HEX_CHARS[s[i] >> 4];
                    chars[pos + 1] = HEX_CHARS[s[i] & 0xF];
                }
                writer.WriteStringValue(new string(chars));
            }

            [MethodImpl(MethodImplOptions.AggressiveInlining)]
            private static int HexVal(char c) => c <= '9' ? c - '0' : (c <= 'F' ? c - 'A' + 10 : c - 'a' + 10);
        }

    }
}
