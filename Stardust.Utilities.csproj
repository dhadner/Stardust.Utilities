<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
  </PropertyGroup>

  <!-- Exclude the Generators subfolder from compilation - it has its own project -->
  <ItemGroup>
    <Compile Remove="Generators\**" />
    <None Remove="Generators\**" />
    <EmbeddedResource Remove="Generators\**" />
  </ItemGroup>

  <!-- Exclude the Test subfolder from compilation - it has its own project -->
  <ItemGroup>
    <Compile Remove="Test\**" />
    <None Remove="Test\**" />
    <EmbeddedResource Remove="Test\**" />
  </ItemGroup>

  <!-- Exclude the Tool subfolder from compilation - it has its own project -->
  <ItemGroup>
    <Compile Remove="Tool\**" />
    <None Remove="Tool\**" />
    <EmbeddedResource Remove="Tool\**" />
  </ItemGroup>
  <ItemGroup>
    <None Include="Stardust.Utilities.Tool\Stardust.Utilities.Tool.csproj" />
  </ItemGroup>

  <!-- Reference the source generator as an analyzer (optional, can be removed if using Tool approach) -->
  <ItemGroup>
    <ProjectReference Include="Generators\Stardust.Generators.csproj" OutputItemType="Analyzer" ReferenceOutputAssembly="false" />
  </ItemGroup>

</Project>
