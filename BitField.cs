using System;

namespace Stardust.Utilities
{
    // Note: The non-generic BitField8/16/32/64 and BitFlag8/16/32/64 types have been removed.
    // Use the [BitFields] source generator for optimal performance with compile-time constants.
    // The generator produces inline bit manipulation code that performs identically to hand-coded solutions.
}
